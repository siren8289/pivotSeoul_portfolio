# FastAPI data directory

This directory is intentionally kept out of Git except for this README and `.gitkeep`.

Place local source datasets, converted CSV files, ZIP archives, Excel files, and PDFs here when running the FastAPI/AI pipeline locally. Large data files should be shared through external storage or regenerated from the documented data source, not committed to the application repository.
